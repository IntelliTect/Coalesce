on:
  push:
    branches:
      - dev

jobs:
  alpha:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: ./.github/workflows/build.yml
        with:
          prereleaseSlug: alpha

