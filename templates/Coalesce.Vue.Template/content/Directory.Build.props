<Project>
  <PropertyGroup>
    <LangVersion>12.0</LangVersion>
    <ImplicitUsings>true</ImplicitUsings>
    <Nullable>enable</Nullable>

    <AccelerateBuildsInVisualStudio>true</AccelerateBuildsInVisualStudio>

    <CoalesceVersion>6.0.4</CoalesceVersion>
    <!-- Disable Source Link attribute in non-library projects to avoid hot reload issues -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>

    <!--#if
    (KeepTemplateOnly) -->
    <DefineConstants>$(DefineConstants);KeepTemplateOnly;AppInsights;OpenAPI;AuditLogs;TrackingBase;ExampleModel</DefineConstants>
    <DefineConstants>$(DefineConstants);Identity;UserPictures;MicrosoftAuth;GoogleAuth;LocalAuth;OtherOAuth</DefineConstants>
    <DefineConstants>$(DefineConstants);EmailAzure;EmailSendGrid;Hangfire;AIChat</DefineConstants>
    <DefineConstants>$(DefineConstants);Tenancy;TenantPerHostname;TenantCreateSelf;TenantCreateExternal;TenantCreateAdmin;TenantMemberInvites</DefineConstants>
    <!--#endif -->
  </PropertyGroup>
</Project>